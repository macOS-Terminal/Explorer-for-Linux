# 角色设定你是一位资深的 Linux 桌面环境研发工程师，精通 C++、Qt/GTK 以及底层显示服务协议（X11 和 Wayland）。请帮我设计并实现一个名为 "Explorer for Linux" 的整蛊/极客项目。



# 项目概述
该项目旨在 Linux 上深度还原 Windows 11 的 Explorer 功能和交互 UI，甚至是其“崩溃体验”。项目包含两个核心组件：
1. `winlogin`（Linux 下的 ELF 可执行文件，为了玩梗命名为 winlogin.exe）
2. `explorer` 主程序
要求兼容多种主流窗口管理器和桌面环境（Niri, Hyprland, XFCE, KDE, GNOME 等）。




# 核心功能需求




## 1. 基础 UI 与资源管理
* 深度还原 Windows 11 资源管理器的 UI 界面。
* 模拟“盘符”概念：将 Linux 的根目录和各个挂载点（Mount points）虚拟映射为 C盘、D盘、E盘 等形式展现。




## 2. winlogin 守护进程与崩溃接管
* `winlogin.exe` 需作为守护进程（Daemon）在前端运行，监控 `explorer` 的状态。
* **崩溃处理**：当 `explorer` 进程崩溃时，`winlogin.exe` 接管屏幕，显示全屏黑屏或白屏。黑白屏取决于读取到的用户当前 Qt/GTK 主题（暗色主题显黑屏，亮色主题显白屏）。
* **快捷键唤醒**：在崩溃黑/白屏状态下，监听 `Super+E` 快捷键。按下后仅重新拉起 `explorer` 窗口，但**不恢复**底层的页面黑/白屏状态。




## 3. “未响应”特效机制（核心还原）
* 监控 `explorer` 的 UI 线程，在发生卡顿时弹出经典的“未响应”半透明遮罩和提示窗口。
* **无限套壳**：如果用户强行点击“未响应”窗口导致再次卡顿，需支持“未响应窗口本身的未响应”（即无限递归套壳生成新的未响应窗口）。




## 4. 强制退出与自启动阻断机制
* **强制返回 TTY**：拦截常规退出快捷键，仅允许通过按下 `Shift+F10` 强制 Kill 相关进程并退回 TTY。
* **强绑定自启动**：将 `winlogin` 和 `explorer` 深度绑定并加入系统/用户的自启动项（需考虑 Systemd user service 或 .desktop autostart）。在启动阶段，若检测到其中任意一个未成功运行，则直接阻断或 Kill 掉整个桌面环境（如结束 Wayland compositor 或 X session）。




# 输出要求（至关重要）
这是一个庞大的工程，为了避免你输出超时或中断，请**严格按照以下步骤与我交互**：
1. **第一步**：先给我项目的整体技术选型（建议使用 Qt 还是 GTK？如何优雅跨越 Wayland/X11 拦截 Shift+F10？如何实现无限套壳的窗口逻辑？）以及项目的文件架构目录。
2. 在我回复“继续”后，再逐步输出 `winlogin.exe` 的守护进程代码、`explorer` 的 UI 代码以及自启动配置。不要一次性输出全部代码！